---
  // This route exists only as an alias to the real homepage at `/`.
  // Keeping it prevents broken links if anything points to `/home`.

  // If you are running Astro with SSR (adapter enabled), this will perform a real HTTP redirect.
  if (typeof Astro !== 'undefined' && Astro?.redirect) {
    return Astro.redirect('/', 302);
  }
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Redirecting…</title>

    <!-- Static-site friendly redirect fallback -->
    <meta http-equiv="refresh" content="0; url=/" />
    <link rel="canonical" href="/" />

    <script>
      // JS redirect fallback (covers cases where meta refresh is blocked)
      window.location.replace('/');
    </script>
  </head>
  <body>
    <p>
      Redirecting to <a href="/">home</a>…
    </p>
  </body>
</html>